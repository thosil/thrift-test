{
  "name": "thrift-test",
  "version": "0.1.0",
  "description": "simple test with thrift",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "make",
    "prebuild": "npm run build"
  },
  "author": "thosil",
  "license": "ISC",
  "dependencies": {
    "thrift": "^0.9.3"
  },
  "keywords": ["thrift"],
  "repository": "https://github.com/thosil/thrift-test.git"
}
